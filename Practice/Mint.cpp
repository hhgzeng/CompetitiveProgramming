#include <bits/stdc++.h>

using i64 = long long;

constexpr int P = 1000000007;

struct Z {
	
}

void solve() {
	int a, b;
	std::cin >> a >> b;
	std::cout << a + b << "\n";
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int T = 1;
    while (T--) {
    	solve();
    }

    return 0;
}