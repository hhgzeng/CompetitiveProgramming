#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 5;

void solve()
{
    int n, k;
    cin >> n >> k;
    cout << n - k + 1 << "\n";
    
}

signed main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr), std::cout.tie(nullptr);

    int T = 1;
    while (T--) {
        solve();
    }

    return 0;
}