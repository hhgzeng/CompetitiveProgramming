#include <bits/stdc++.h>

using namespace std;

const int N = 1e5 + 5;

void solve()
{
    
    
}

signed main()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr), std::cout.tie(nullptr);

    int T = 1;
    cin >> T;
    while (T--) {
        solve();
    }

    return 0;
}